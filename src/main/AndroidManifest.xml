<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.driver.profitcalculator">

    <!--
        Silent Eye - Core Permissions
        NO ACCESSIBILITY_SERVICE - avoids banking app blocks
        NO UI_AUTOMATOR - avoids Rapido detection
    -->
    
    <!-- Screen capture via MediaProjection -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    
    <!-- Ghost overlay - SYSTEM_ALERT_WINDOW -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Wake lock for continuous operation -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Internet for ML Kit model download (one-time) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProfitCalculator"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Silent Eye Foreground Service
            Captures screen at 2 FPS, processes with ML Kit
        -->
        <service
            android:name=".ScreenCaptureServiceV2"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection"
            android:stopWithTask="false" />

    </application>

</manifest>
